{% block navbar %}

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('home_page') }}"> Grolidon </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('boutique') }}" > {{ 'Default.navbar.boutique' | trans }}  <span class="sr-only">(current)</span></a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('contact') }}">  {{ 'Default.navbar.contact' | trans }} </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('panier_index') }}">  {{ 'Default.navbar.panier' | trans }} ({{ nbArticles }}) </a>
                </li>

                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}"> Déconnexion </a>
                </li>

                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}"> Connexion </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('usager_inscription') }}">  Inscription </a>
                </li>
                {% endif %}

            </ul>



            <form class="form-inline my-2 my-lg-0" action="{{ path('search') }}" method="GET">
                <input class="form-control mr-sm-2" type="search" placeholder="{{ 'Default.navbar.rechercher' | trans }}" aria-label="Search" name="searchProduct">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit" value="Submit"> {{ 'Default.navbar.rechercher' | trans }} </button>
            </form>

            <div class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <img src="{{ asset('images/icon/langageIcon.png') }}" style="width: 20px; height: 20px;">
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('home_page', {"_locale":'en'})  }}">En</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('home_page', {"_locale":'fr'})  }}">Fr</a>
                </div>
            </div>

            <div class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <img src="{{ asset('images/icon/deviseIcon.png') }}" style="width: 20px; height: 20px;">
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">

                    <a class="dropdown-item" href="{{ path("devise-change", {'to': 'USD'}) }}">USD</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path("devise-change", {'to': 'EUR'}) }}">EUR</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path("devise-change", {'to': 'GPB'}) }}">GPB</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path("devise-change", {'to': 'PHP'}) }}">PHP</a>

                </div>
            </div>
        </div>
    </nav>

{% endblock %}
